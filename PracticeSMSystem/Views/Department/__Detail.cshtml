@model PracticeSMSystem.Data.Models.Department
@{
    Layout = null;
}
@* <div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">Department Details</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body">
            <dl class="row">
                <dt class="col-sm-4">Department Name</dt>
                <dd class="col-sm-8">@Model.DName</dd>

                <dt class="col-sm-4">Description</dt>
                <dd class="col-sm-8">@Model.DDescription</dd>

                <dt class="col-sm-4">Head</dt>
                <dd class="col-sm-8">
                    @(Model.Head != null ? $"{Model.Head.FirstName} {Model.Head.LastName}" : "No Head Assigned")
                </dd>
            </dl>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        </div>
    </div>
</div>

 *@


 @* <div class="modal fade" id="deptEditModal" tabindex="-1" role="dialog" aria-labelledby="deptEditModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <form asp-controller="Department" asp-action="Update" method="post" class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deptEditModalLabel">Edit Department</h5>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <div class="modal-body">
                <input type="hidden" name="Id" value="@Model.Id" />

                <div class="form-group">
                    <label for="DName">Name</label>
                    <input type="text" class="form-control" name="DName" value="@Model.DName" />
                </div>

                <div class="form-group">
                    <label for="DDescription">Description</label>
                    <textarea class="form-control" name="DDescription">@Model.DDescription</textarea>
                </div>

                <div class="form-group">
                    <label for="DepHeadId">Head of Department</label>
                    <select class="form-control" name="DepHeadId">
                        <option value="">-- Select Head --</option> *@

                        @foreach (PracticeSMSystem.Data.Models.Staff staff in ViewBag.StaffList)
                        {
                            <option value="@staff.Id" selected="@(staff.Id == Model.DepHeadId)">
                                @staff.FullName
                               @*  @staff.FirstName @staff.LastName *@
                            </option>
                        }
                   @*  </select>
                </div>
            </div>
                <div class="modal-footer">
                <button type="submit" class="btn btn-primary">Update</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
            </div>
        </form>
    </div>
</div>  *@
